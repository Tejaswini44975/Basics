class Solution{
    public List<Integer> swapNumbers(int a, int b) {
        List<Integer> result = new ArrayList<>();
        result.add(b);
        result.add(a);

        return result;
        
    }
    
}